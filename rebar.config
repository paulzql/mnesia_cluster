{erl_opts, [debug_info]}.
{deps, [
       %% auto reconnect mnesia and merge table
       %% note: mnesia not reconnect downed nodes, so we need reconnect and merge manualy
       {reunion, {git, "https://github.com/snar/reunion.git", {branch, "master"}}}
       ]}.